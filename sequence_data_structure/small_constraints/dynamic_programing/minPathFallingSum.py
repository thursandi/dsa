def minFallingPathSum(self, matrix: List[List[int]]) -> int:
    """
        Find the minimum sum of a falling path through the matrix.
        A falling path starts at any element in the first row and chooses 
        one element from each row. The next row's choice must be in a column 
        that is different from the previous row's column by at most one.
      
        Args:
            matrix: n x n matrix of integers
          
        Returns:
            The minimum sum of any falling path through the matrix
    """

    # this could be category of Max/min problems using dynamic programming
    return 0
    